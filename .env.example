# Copy this file to .env before the first "docker compose up -d". The .env file is not committed.
# SQL Server refuses to start unless the password is at least 8 characters and mixes upper case,
# lower case and digits or symbols. This value is only ever used by the local container.
MSSQL_SA_PASSWORD=change-me-Local1
